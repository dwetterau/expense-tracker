{{# with expense }}
  <h1> {{ this.title }} </h1>
  {{# if this.description }}
    <p> {{ this.description }} </p>
  {{/if }}
  <p> value: {{format_money this.value }} </p>
  {{> participant-list }}
  {{# if this.receipt_image }}
    {{! TODO: factor out this path somehow }}
    <img src="/thumb/{{ this.receipt_image }}/640x480" />
  {{/if}}
{{/with}}